@model Exam1.Models.Account

@{
    ViewBag.Title = "PersonalCabinetAuctions";
}

<h2>Personal Cabinet</h2>

@Html.ActionLink("Products", "PersonalCabinet")
@Html.ActionLink("Bets", "PersonalCabinetBets")

<h3>Your auctions</h3>
@Html.ActionLink("Add new product", "CreateProductInfo")
@if (!Model.ProductsToSell.Where(p => p.IsAuction == "auction").Any())
{
    <p>You dont have any auctions yet</p>
}

<div class="container">
    @foreach (var product in Model.ProductsToSell.Where(p => p.IsAuction == "auction"))
    {
        <div class="row">
            <img class="col-md-2 h-100 d-inline-block" src="@product.Photo" alt="Alternate Text" />
            <div class="col-md-7">
                <h5>@product.Name</h5>
                <p>@(product.Description.Length>150?product.Description.Substring(0,150)+"...":product.Description)</p>
            </div>
            <div class="col-md-3">
                <p>Current price @product.Price</p>
                @Html.ActionLink("Finish", "FinishAuction", "Customer", new { product.Id }, null)
                @Html.ActionLink("Details", "Details", "Customer", new { product.Id }, null)
            </div>
        </div>

    }
</div>

@*<ul>
        @foreach (var product in Model.ProductsToSell.Where(p => p.IsAuction == "auction"))
        {
            <li>
                @product.Name @Html.ActionLink("Update", "UpdateProductInfo", "Customer", new { product.Id }, null)
                @Html.ActionLink("Finish", "FinishAuction", "Customer", new { product.Id }, null)
            </li>
        }
    </ul>*@


